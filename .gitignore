# local scratch (keep directory via state/.gitkeep)
state/*
!state/.gitkeep

# harness iteration logs (adr-08) — keep folder, ignore files
log/*
!log/.gitkeep
*.log
.DS_Store

# vault index (local)
.mvmcp/

# editor / agent noise
.idea/
.vscode/
*.swp
*~

# never commit secrets
.env
.env.*

# runtime / OS
__pycache__/
*.pyc
node_modules/
